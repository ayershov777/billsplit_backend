const router = require('express').Router();
const groupsCtrl = require('../controllers/groups');

// router.get('/', groupsCtrl.index);
// router.get('/:title', groupsCtrl.show);
// router.post('/', groupsCtrl.create);
// router.put('/:title', groupsCtrl.update);
// router.delete('/:title', groupsCtrl.remove);

module.exports = router;
