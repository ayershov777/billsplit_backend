const router = require('express').Router();
const notificationsCtrl = require('../controllers/notifications');

// router.post('/', notificationsCtrl.create);
// router.delete('/:id', notificationsCtrl.remove);

module.exports = router;
